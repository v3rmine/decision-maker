#!/usr/bin/env rust-script
//! ```cargo
//! [dependencies]
//! duct = "0.13"
//! ```

extern crate duct;
use std::error::Error;

use duct::cmd;

fn main() -> Result<(), Box<dyn Error>> {
    Ok(())
}
